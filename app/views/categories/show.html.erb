<h1><%= @category.title %></h1>

<p>This is a list of all the services lying in the <%= @category.title %> category.</p>

<h2>Category Description</h2>

<p><%= @category.description %></p>

<table class="table table-striped table-bordered">
	<tr>
		<th>Service</th>
		<th>Offered by</th>
	</tr>
	<% @category.services.each do |s| %>
	<tr>
		<td><%= link_to s.title, s %></td>
		<td><%= link_to s.user.username, s.user %></td>
	</tr>
	<% end %>
</table>
